# 安全政策

## 报告安全问题

请不要在公开 Issue 中发布 API Key、数据库密码、内部地址或可复现的生产漏洞。建议通过 GitHub 私密联系方式联系仓库维护者，并提供影响范围、复现步骤和临时缓解办法。

## 项目安全边界

- DeepSeek、数据库和消息队列凭证只通过环境变量或密钥管理服务注入。
- 修复执行器默认是 dry-run；allowlist 模式只执行显式参数列表。
- 生产部署前必须启用 API Key 认证、TLS、网络策略、最小权限和审计留存。
- 示例拓扑、CMDB 和测试数据不代表真实生产环境。
